<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Expense Tracker</title>
    {{!-- BOOTSTRAP --}}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" 
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

</head>
<body>
    {{!-- HEADER --}}

    <div class="container-fluid bg-body-tertiary">
        <div class="row">
    
            <div class="col-md-6 mx-auto my-3 d-flex justify-content-around">
                <span class="navbar-brand h1 fs-1 text-success">EXPENSE TRACKER</span>
            </div>
            <div class="col-md-6 my-3 d-flex justify-content-around">
                <ul class="navbar-nav">
                    {{#if loggedIn}}
                    <li class="nav-item">
                        <a id="logout" class="btn btn-outline-success" aria-current="page" href="/logout">Logout</a>
                    </li>
                    {{else}}
                    <li class="nav-item">
                        <a id="login" class="btn btn-outline-success" aria-current="page" href="/login">Login</a>
                    </li> 
                    {{/if}}
            </div>
    
        </div>
    </div>

    <main class="container-fluid">
        {{{ body }}}
    </main>
    

<script src="/js/logout.js"></script>   
</body>
</html>
